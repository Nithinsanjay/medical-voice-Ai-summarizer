import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF5B22C4);
  static const background = Color(0xFFF7F5FF);
  static const card = Colors.white;
  static const surface = Color(0xFFF0EEFF);
}

class AppStrings {
  static const appTitle = 'Voice AI';
  static const appSubtitle = 'AI Medical Voice Prescription Summarizer';
}
